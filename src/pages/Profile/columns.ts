const commonOptions = [
  [
    {
      Header: 'Age',
      accessor: 'age',
      width: 50,
    },
  ],
];

const columnOptions = {
  income: [...commonOptions],
  balance: [],
  cash: [],
};

export default columnOptions;
